/**
 * The Superadmin repair surface (ADR-0046): the Reindex page and the client it drives.
 * The `admin` translation scope ships from the `/i18n` entry point (ADR-0049).
 */
export * from './pages/admin.page';
export * from './services/admin.client';
