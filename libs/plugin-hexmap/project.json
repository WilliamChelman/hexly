{
  "name": "plugin-hexmap",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/plugin-hexmap/src",
  "prefix": "app",
  "projectType": "library",
  "tags": ["scope:web", "type:feature"],
  "targets": {
    "test": {
      "executor": "@angular/build:unit-test",
      "options": {
        "watch": false,
        "buildTarget": "web:build:development",
        "tsConfig": "libs/plugin-hexmap/tsconfig.spec.json",
        "setupFiles": ["libs/plugin-hexmap/src/test-setup.ts"]
      }
    }
  }
}
