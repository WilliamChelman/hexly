services:
  hexly:
    image: ghcr.io/williamchelman/hexly:latest
    ports:
      - "3000:3000"
    volumes:
      # The TrailBase depot (DB, config.textproto, uploads). DATA_DIR=/data is
      # baked into the image.
      - ./hexly-data:/data
    restart: unless-stopped
