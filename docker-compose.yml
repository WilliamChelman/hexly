services:
  hexly:
    image: ghcr.io/williamchelman/hexly:latest
    ports:
      - "3000:3000"
    volumes:
      - ./hexly-data:/data
    environment:
      HEXLY_DIR: /data
    restart: unless-stopped
